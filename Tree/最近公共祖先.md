```cpp
std::vector<int> dep(n + 1);
std::vector<std::vector<int>> fa(21, std::vector<int> (n + 1));
auto dfs = [&] (auto &&self, int p, int father) -> void {
    dep[p] = dep[father] + 1;
    fa[0][p] = father;
    for (int i = 1; i <= 20; i++) {
        fa[i][p] = fa[i - 1][fa[i - 1][p]];
    }
    for (auto c : edge[p]) {
        if (c != father) {
            self(self, c, p);
        }
    }
};

dfs(dfs, root, 0);
auto lca = [&] (int u, int v) -> int {
    if (dep[u] < dep[v]) {
        std::swap(u, v);
    }
    for (int d = dep[u] - dep[v], i = 0; d; d >>= 1, i++) {
        if (d & 1) {
            u = fa[i][u];
        }
    }
    if (u == v) {
        return u;
    }
    for (int i = 20; i >= 0; i--) {
        if (fa[i][u] != fa[i][v]) {
            u = fa[i][u];
            v = fa[i][v];
        }
    }
    return fa[0][u];
};
```
