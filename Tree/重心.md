```cpp
std::vector<i64> dep(n + 1), siz(n + 1), dp(n + 1);
auto dfs1 = [&] (this auto &&self, int p, int fa) -> void {
    dep[p] = dep[fa] + 1;
    siz[p] = w[p];
    dp[1] += (dep[p] - 1) * w[p];
    for (auto c : edge[p]) {
        if (c != fa) {
            self(c, p);
            siz[p] += siz[c];
        }
    }
};  

dfs1(1, 0);
auto dfs2 = [&] (this auto &&self, int p, int fa) -> void {
    for (auto c : edge[p]) {
        if (c != fa) {
            dp[c] = dp[p] + siz[1] - 2 * siz[c];
            self(c, p);
        }
    }
};
dfs2(1, 0);
int ans = std::min_element(dp.begin() + 1, dp.end()) - dp.begin();
```
